server.port=8080
spring.application.name=simple-spring-app

# Optional: Set context path if needed
# server.servlet.context-path=/myapp

# Disable thymeleaf if not using
spring.thymeleaf.enabled=false